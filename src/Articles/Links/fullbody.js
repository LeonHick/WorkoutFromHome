import config from "../../config";

const links = {
  toPPLRoutine: config.routineUrlStructure("ppl"),
  toBodyweightRoutine: config.routineUrlStructure("bodyweight"),
  toAmazonDumbbells: "http://amazon.com",
  toEssentialEquipmentReview: "review",
};

export default links;
